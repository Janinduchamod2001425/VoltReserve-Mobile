<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Camera is optional (works on devices without a camera too) -->
    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@drawable/logo_rounded"
        android:supportsRtl="true"
        android:theme="@style/Theme.VoltReserve">

        <!-- Maps key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />

        <!-- Activities -->
        <activity
            android:name=".OperatorProfileActivity"
            android:exported="false" />
        <activity
            android:name=".OperatorDashboardActivity"
            android:exported="false" />
        <activity
            android:name=".ReservationListActivity"
            android:exported="false" />
        <activity
            android:name=".ReservationActivity"
            android:exported="false" />
        <activity
            android:name=".OwnerProfileActivity"
            android:exported="false" />
        <activity
            android:name=".RegisterStep1Activity"
            android:exported="false" />
        <activity
            android:name=".RegisterStep2Activity"
            android:exported="false" />
        <activity
            android:name=".RegisterStep3Activity"
            android:exported="false" />
        <activity
            android:name=".DashboardActivity"
            android:exported="false" />
        <activity
            android:name=".MapActivity"
            android:exported="false"
            android:label="Charging Stations" />
        <activity
            android:name=".LoginActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- ✅ Add QR scanner & reservation detail screens -->
        <activity
            android:name=".QrScannerActivity"
            android:exported="false" />
        <activity
            android:name=".ReservationDetailActivity"
            android:exported="false"
           />

        <!-- Launcher -->
        <activity
            android:name=".LogoActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
